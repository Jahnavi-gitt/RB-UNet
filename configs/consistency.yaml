# U-Net + Corruption Consistency Configuration
# Experiment 3
defaults:
  - config.yaml

model:
  name: "rb_unet"
  features: [16, 32, 64, 128, 256]
  boundary_head_channels: 16

loss:
  dice_weight: 1.0
  bce_weight: 1.0
  boundary_weight: 0.0
  consistency_weight: 0.5
